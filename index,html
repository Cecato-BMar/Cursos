<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transformação Digital - Profissão do Futuro</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 50%, #16213e 100%);
            color: white;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            background: radial-gradient(circle at 20% 80%, #0d4377 0%, transparent 50%),
                        radial-gradient(circle at 80% 20%, #14a085 0%, transparent 50%),
                        radial-gradient(circle at 40% 40%, #16213e 0%, transparent 50%);
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse"><path d="M 10 0 L 0 0 0 10" fill="none" stroke="%23ffffff" stroke-width="0.5" opacity="0.1"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
        }

        .hero-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
            z-index: 2;
            position: relative;
        }

        .hero-text {
            animation: slideInLeft 1s ease-out;
        }

        .hero-badge {
            background: linear-gradient(45deg, #14a085, #0d4377);
            padding: 10px 20px;
            border-radius: 25px;
            font-size: 14px;
            font-weight: bold;
            display: inline-block;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 900;
            margin-bottom: 20px;
            background: linear-gradient(45deg, #ffffff, #14a085);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            line-height: 1.2;
        }

        .hero p {
            font-size: 1.2rem;
            margin-bottom: 30px;
            opacity: 0.9;
        }

        .cta-form {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group input,
        .form-group select {
            width: 100%;
            padding: 15px;
            border: none;
            border-radius: 8px;
            background: rgba(255, 255, 255, 0.1);
            color: white;
            font-size: 16px;
            backdrop-filter: blur(5px);
        }

        .form-group input::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }

        .cta-button {
            background: linear-gradient(45deg, #14a085, #0d4377);
            color: white;
            padding: 18px 40px;
            border: none;
            border-radius: 50px;
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            width: 100%;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(20, 160, 133, 0.4);
        }

        .countdown-text {
            text-align: center;
            font-size: 14px;
            opacity: 0.8;
            margin-top: 15px;
        }

        .hero-image {
            text-align: center;
            animation: slideInRight 1s ease-out;
        }

        .instructor-photo {
            width: 350px;
            height: 400px;
            background: linear-gradient(135deg, #14a085, #0d4377);
            border-radius: 20px;
            margin: 0 auto;
            position: relative;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }

        .instructor-photo::before {
            content: '👨‍💻';
            font-size: 120px;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        /* Features Section */
        .features {
            padding: 100px 0;
            background: rgba(0, 0, 0, 0.3);
        }

        .features h2 {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 60px;
            color: #14a085;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 30px;
            text-align: center;
            transition: all 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            border-color: #14a085;
        }

        .feature-icon {
            width: 60px;
            height: 60px;
            background: linear-gradient(45deg, #14a085, #0d4377);
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
        }

        .feature-card h3 {
            font-size: 1.3rem;
            margin-bottom: 15px;
            color: #14a085;
        }

        /* Target Audience */
        .target-audience {
            padding: 100px 0;
        }

        .target-audience h2 {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 60px;
        }

        .audience-steps {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .step-card {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            padding: 30px;
            position: relative;
        }

        .step-number {
            position: absolute;
            top: -15px;
            left: 30px;
            background: linear-gradient(45deg, #14a085, #0d4377);
            color: white;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 18px;
        }

        .step-card h3 {
            margin-top: 15px;
            margin-bottom: 15px;
            color: #14a085;
        }

        /* Instructor Section */
        .instructor {
            padding: 100px 0;
            background: rgba(0, 0, 0, 0.3);
        }

        .instructor-content {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 60px;
            align-items: center;
        }

        .instructor-info h2 {
            font-size: 2.5rem;
            margin-bottom: 30px;
            color: #14a085;
        }

        .instructor-photo-large {
            width: 300px;
            height: 350px;
            background: linear-gradient(135deg, #14a085, #0d4377);
            border-radius: 20px;
            position: relative;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
        }

        .instructor-photo-large::before {
            content: '👨‍💼';
            font-size: 100px;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        /* Countdown Section */
        .countdown-section {
            padding: 100px 0;
            text-align: center;
        }

        .countdown-section h2 {
            font-size: 2.5rem;
            margin-bottom: 50px;
        }

        .countdown {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-bottom: 50px;
        }

        .countdown-item {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 15px;
            padding: 30px;
            min-width: 120px;
        }

        .countdown-number {
            font-size: 3rem;
            font-weight: bold;
            color: #14a085;
            display: block;
        }

        .countdown-label {
            font-size: 14px;
            opacity: 0.8;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* Animations */
        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-content {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .hero h1 {
                font-size: 2.5rem;
            }

            .instructor-content {
                grid-template-columns: 1fr;
                text-align: center;
            }

            .countdown {
                flex-wrap: wrap;
                gap: 15px;
            }

            .countdown-item {
                min-width: 100px;
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <div class="hero-content">
                <div class="hero-text">
                    <div class="hero-badge">🚀 Transformação Digital</div>
                    <h1>DOMINE AS HABILIDADES DO FUTURO</h1>
                    <p>Depois de 5 anos nesse mercado, mais de 10k alunos transformados, eu vou te revelar tudo o que você precisa para se diferenciar e viver da sua profissão - mesmo saindo do zero.</p>
                    <p><strong>Economize muito dinheiro e anos da sua vida desenvolvendo as habilidades certas!</strong></p>
                    
                    <div class="cta-form">
                        <div class="form-group">
                            <input type="text" placeholder="Seu melhor nome" required>
                        </div>
                        <div class="form-group">
                            <input type="email" placeholder="Seu melhor email" required>
                        </div>
                        <div class="form-group">
                            <select required>
                                <option value="">Quero participar!</option>
                                <option value="presencial">Presencial</option>
                                <option value="online">Online</option>
                            </select>
                        </div>
                        <button class="cta-button">QUERO PARTICIPAR</button>
                        <div class="countdown-text">⏰ Das 19 às 22h • Inscrições até 15/12/2025</div>
                    </div>
                </div>
                
                <div class="hero-image">
                    <div class="instructor-photo"></div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features">
        <div class="container">
            <h2>O QUE VOCÊ VAI APRENDER</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">💡</div>
                    <h3>Inovação</h3>
                    <p>Como trabalhar em projetos únicos ou reinventar processos para ter sempre algo novo para entregar.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🎯</div>
                    <h3>Estratégia</h3>
                    <p>Os principais de detecção da estratégia de marca mais aplicados pelos grandes influencers e marcas.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">⚙️</div>
                    <p>Como automatizar seus processos e ferramentas que uma empresa de verdade precisa implementar.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🤝</div>
                    <h3>Networking</h3>
                    <p>Como se relacionar no mercado, conquistar clientes e criar uma rede de contatos sólida.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Target Audience -->
    <section class="target-audience">
        <div class="container">
            <h2>ESTE EVENTO É OBRIGATÓRIO PARA QUEM</h2>
            <div class="audience-steps">
                <div class="step-card">
                    <div class="step-number">01</div>
                    <h3>Iniciantes</h3>
                    <p>É iniciante e quer começar do jeito certo no mercado da profissão.</p>
                </div>
                <div class="step-card">
                    <div class="step-number">02</div>
                    <h3>Experiência</h3>
                    <p>Já trabalha como designer, mas não consegue cobrar mais de R$3000 pelos trabalhos.</p>
                </div>
                <div class="step-card">
                    <div class="step-number">03</div>
                    <h3>Crescimento</h3>
                    <p>Quer se especializar no mercado, desenvolvendo habilidades técnicas de alto valor agregado.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Instructor Section -->
    <section class="instructor">
        <div class="container">
            <div class="instructor-content">
                <div class="instructor-photo-large"></div>
                <div class="instructor-info">
                    <h2>COM QUEM VOCÊ VAI APRENDER</h2>
                    <p>O Alex Doe era apenas mais um jovem perdido estudando Direito quando tudo mudou para ele em 2019.</p>
                    <p>Estudou Design sozinho como arte em uma pequena cidade de 5 mil habitantes do interior do Estado do Rio de Janeiro que na época desconhecia Miguel Pereira, morou sozinho por 2 anos para conseguir trabalhar dando desenho técnico e descobrir a sua área.</p>
                    <p>Agora, ele quer ajudar seus estudantes e mostrar tudo o que vem aprendendo e aplicando durante todos esses anos nas suas consultorias no mercado.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Countdown Section -->
    <section class="countdown-section">
        <div class="container">
            <h2>O EVENTO COMEÇA EM...</h2>
            <div class="countdown">
                <div class="countdown-item">
                    <span class="countdown-number" id="days">25</span>
                    <span class="countdown-label">Dias</span>
                </div>
                <div class="countdown-item">
                    <span class="countdown-number" id="hours">08</span>
                    <span class="countdown-label">Horas</span>
                </div>
                <div class="countdown-item">
                    <span class="countdown-number" id="minutes">45</span>
                    <span class="countdown-label">Minutos</span>
                </div>
                <div class="countdown-item">
                    <span class="countdown-number" id="seconds">30</span>
                    <span class="countdown-label">Segundos</span>
                </div>
            </div>
            <button class="cta-button" style="max-width: 400px; margin: 0 auto;">QUERO PARTICIPAR</button>
        </div>
    </section>

    <script>
        // Countdown Timer Animation
        function animateCountdown() {
            const days = document.getElementById('days');
            const hours = document.getElementById('hours');
            const minutes = document.getElementById('minutes');
            const seconds = document.getElementById('seconds');
            
            setInterval(() => {
                // Simulate countdown animation
                let currentSeconds = parseInt(seconds.textContent);
                currentSeconds = currentSeconds > 0 ? currentSeconds - 1 : 59;
                seconds.textContent = currentSeconds.toString().padStart(2, '0');
                
                if (currentSeconds === 59) {
                    let currentMinutes = parseInt(minutes.textContent);
                    currentMinutes = currentMinutes > 0 ? currentMinutes - 1 : 59;
                    minutes.textContent = currentMinutes.toString().padStart(2, '0');
                    
                    if (currentMinutes === 59) {
                        let currentHours = parseInt(hours.textContent);
                        currentHours = currentHours > 0 ? currentHours - 1 : 23;
                        hours.textContent = currentHours.toString().padStart(2, '0');
                        
                        if (currentHours === 23) {
                            let currentDays = parseInt(days.textContent);
                            currentDays = currentDays > 0 ? currentDays - 1 : 30;
                            days.textContent = currentDays.toString().padStart(2, '0');
                        }
                    }
                }
            }, 1000);
        }

        // Form submission
        document.querySelectorAll('.cta-button').forEach(button => {
            button.addEventListener('click', function(e) {
                e.preventDefault();
                this.textContent = 'INSCRITO! ✓';
                this.style.background = 'linear-gradient(45deg, #28a745, #20c997)';
                setTimeout(() => {
                    this.textContent = 'QUERO PARTICIPAR';
                    this.style.background = 'linear-gradient(45deg, #14a085, #0d4377)';
                }, 2000);
            });
        });

        // Start countdown animation
        animateCountdown();

        // Add scroll animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        document.querySelectorAll('.feature-card, .step-card').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(20px)';
            el.style.transition = 'all 0.6s ease';
            observer.observe(el);
        });
    </script>
</body>
</html>
